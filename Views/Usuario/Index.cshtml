
@{
    ViewBag.Title = "Index";
}


@model Notado.ViewModels.UsuarioViewModel

<div class="card">
    <div class="card-body">
        <h1 class="card-title">Usuários</h1><br />
        <a href="/Usuario/Adicionar" class="btn btn-success">Adicionar</a><br /><br />

        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Usuário</th>
                    <th scope="col">Autorização</th>
                    <th scope="col">Excluir</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var Usuario in ViewBag.Usuarios)
                {
                    <tr class="busca">
                        <th scope="row">@Usuario.Nome</th>
                        <td class="info-nome">@Usuario.Autorizacao</td>
                        <td> <a href="/Usuario/Excluir/@Usuario.Id" class="btn btn-danger">Excluir</a></td>

                        <!-- Modal -->
                        @*<div class="modal fade" id="myModal" role="dialog">
                            <div class="modal-dialog">

                              
                                <div class="modal-content justify-content-center">
                                    <div class="modal-header">
                                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                                        <h4 class="modal-title"></h4>
                                    </div>
                                    <div class="modal-body">
                                        <p>O item será excluído permanentemente. Deseja continuar?</p>
                                    </div>
                                    <div class="modal-footer">
                                        <a href="/Usuario/Excluir/@Usuario.Id" class="btn btn-danger">Excluir</a>
                                        <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
                                    </div>
                                </div>

                            </div>
                        </div>*@

                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>